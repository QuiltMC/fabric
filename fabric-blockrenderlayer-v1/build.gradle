archivesBaseName = "quilted-fabric-blockrenderlayer-v1"
version = getSubprojectVersion(project)
upstream_version = getSubprojectUpstreamVersion(project)

moduleDependencies(project, [
        'fabric-api-base'
])

dependencies {
	include modApi(getQslModule("block", "block_extensions", "1.0.0"))
}
